# Override de DEV — HMR SO DO FRONT (Stack B). Use com o base:
#   docker compose -f docker-compose-2.yml -f docker-compose-2.web.yml up -d --build
# So o 'web' vira Vite dev server (recarrega ao editar .tsx). A api/dispatcher seguem
# build normal (sem dotnet watch) — ao mexer em C#, rode: rebuild-backend.cmd
services:
  web:
    build:
      context: ./src/mtrxsys-web
      dockerfile: Dockerfile
      target: dev
    # Vite na MESMA porta publicada (5174) pro websocket de HMR bater certo.
    ports: !override
      - "${MTRX_BIND:-127.0.0.1}:5174:5174"
    volumes:
      - ./src/mtrxsys-web:/app
      - /app/node_modules
    environment:
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
      VITE_API_URL: "http://localhost:5081"
      VITE_APP_LABEL: "B"
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5174"]
